- hosts: svcs
  tasks:
  - name: Create /etc/coredns directory
    file:
      path: /etc/coredns
      state: directory
  - name: Make a Zonefile
    template:
      src: templates/zonefile.j2
      dest: /etc/coredns/{{ localDomainSuffix }}.zone
